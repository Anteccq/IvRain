<Page
    x:Class="IvRain.Views.InitializeDataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IvRain.Views"
    xmlns:vm="using:IvRain.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="using:IvRain.Views.Converter"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <converter:InverseValueConverter x:Key="Inverse"/>
    </Page.Resources>
    <Page.DataContext>
        <vm:InitializeDataPageViewModel/>
    </Page.DataContext>

    <Grid>
        <StackPanel Orientation="Vertical"  Margin="50, 0">
            <StackPanel.Resources>
                <!--なぜ明示的な型指定が必要なのか(Controlではだめなのか)我々は以下略-->
                <Thickness x:Key="ItemMargin">0,10</Thickness>
                <Style TargetType="PasswordBox">
                    <Setter Property="Margin" Value="{StaticResource ItemMargin}"/>
                </Style>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="{StaticResource ItemMargin}"/>
                </Style>
            </StackPanel.Resources>
            <TextBlock Text="Password must be at least 5 characters long."/>
            <PasswordBox x:Name="FirstPasswordBox" Password="{Binding FirstPassword.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <PasswordBox x:Name="SecondPasswordBox" Password="{Binding SecondPassword.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Button HorizontalAlignment="Right" x:Name="RegisterButton"  Content="Register" IsEnabled="{Binding IsRegistrable.Value}"/>
        </StackPanel>
    </Grid>
</Page>
