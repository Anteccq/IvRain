<UserControl
    x:Class="IvRain.Views.Controls.PasswordView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IvRain.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock FontSize="28" x:Name="TextBlock" />

        <Grid Margin="0, 80, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock FontSize="12" Foreground="Gray" Text="パスワード"/>
            <StackPanel Grid.Row="1"  Orientation="Horizontal" Visibility="Collapsed">
                <local:PasswordViewBox FontSize="22" FontFamily="Courier New" MaxWidth="500" x:Name="PasswordViewBox"/>
                <Button x:Name="CopyButton" FontSize="16" Margin="30, 0"   CornerRadius="10"  Content="Copy" />
            </StackPanel>
            <StackPanel Grid.Row="1">
                <TextBox x:Name="PasswordEditBox" HorizontalAlignment="Left"  Width="400" />
                <StackPanel  Orientation="Horizontal" Margin="0, 30, 0, 0">
                    <Button Content="Save" />
                    <Button Margin="20, 0, 0, 0"  Content="Cancel" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
