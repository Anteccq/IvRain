<UserControl
    x:Class="IvRain.Views.Controls.PasswordView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IvRain.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock FontSize="28" x:Name="TextBlock" />

        <Grid Margin="0, 80, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock FontSize="12" Foreground="Gray" Text="パスワード"/>
            <StackPanel Grid.Row="1"  Orientation="Horizontal">
                <local:PasswordViewBox FontSize="22" FontFamily="Courier New" Width="300" x:Name="PasswordViewBox"/>
                <Button FontSize="16" Margin="10, 0"   CornerRadius="10"  Content="Copy" />
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
